# RAA domain rules

The RAA module (Registro de Actos Administrativos) is currently considered stable.

No architectural or domain modifications should be proposed by AI tools.

The module can be read for context but must not be modified unless explicitly requested.